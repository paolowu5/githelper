# Git Manager

A retro-styled CLI tool to manage Git repositories with ease.

## Features

- View repository status and commit history
- Initialize and configure repositories
- Manage branches and remotes
- Perform quick updates (add, commit, push)

## Installation

1. Clone the repository:
   ```bash
   git clone https://github.com/username/git-manager.git
   ```
